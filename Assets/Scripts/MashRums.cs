using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MashRums : MonoBehaviour {

    //private void OnTriggerEnter2D(Collider2D collision)//when other colliders enter into my collider
    //{
    //    if (collision.CompareTag("Player"))//when collider with Player tag enter into my collider
    //    {
    //        collision.GetComponent<Rigidbody2D>().velocity = Vector2.zero;//clear player velocity
    //        collision.GetComponent<Rigidbody2D>().AddForce(Vector2.one*500f);//add constant force to the player
    //    }
    //}
}
